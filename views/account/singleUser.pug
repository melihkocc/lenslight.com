extends ../layouts/main-layout.pug
block title 
    title Profile | #{user.name}
block content 
    .customers
        .container
            .row
                .col-md-12
                    .titlepage.text_align_center
                        h2 #{user.name}
            .row
                .col-md-12
                    .satteb.text_align_center
                        p.
                            fact that a reader will be distracted by the readable content of a page when looking at its layout.
                            The point of using Lorem Ipsum is that itfact that a reader will be distracted by the readable content of a page when looking at its layout.
                            The point of using Lorem Ipsum is that itfact that a reader will be distracted by the readable content of a page when looking at its layout.
                            The point of using Lorem Ipsum is that itfact that a reader will be distracted by the readable content of a page when looking at its layout.
                            The point of using Lorem Ipsum is that it
                        h3 Profil Fotoğrafı
                        i
                        img(src=`${user.profilePhotoUrl}`, alt='#')
    if inFollowers
        a.btn.btn-primary.d-block.mx-auto.w-25(href=`/unfollow/${user._id}`) Takipten Çık  
    else 
        a.btn.btn-primary.d-block.mx-auto.w-25(href=`/follow/${user._id}`) Takip Et  

    .team
        .container
            .row
                .col-md-12
                    .titlepage.text_align_center
                        h2 #{user.name} Photos
                each photo in photos
                    .col-lg-4.col-md-6.ma_bottom30
                        .lightbox
                            img(src=`${photo.url}`, alt=`${photo.name}`)
                            .view_main
                                .pose
                                    a.read_more(href=`/photo/${photo._id}`)
                                        img(src="/images/ga.png", alt="#")
                                    p more-or-less normal distribution of
                                        br
                                        | letters, as opposed to using



